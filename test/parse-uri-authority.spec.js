'use strict'
const chai = require('chai')
const expect = chai.expect
const parseURIAuthority = require('../lib/parse-uri-authority')

describe('parseURIAuthority', function() {
  it('should have tests written for it!')
})
