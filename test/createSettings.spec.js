'use strict'
const chai = require('chai')
const expect = chai.expect
const createSettings = require('../lib/internal/createSettings')

describe('createSettings', function() {
  it('should have tests written for it!')
})
