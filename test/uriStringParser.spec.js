'use strict'
const chai = require('chai')
const expect = chai.expect
const uriStringParser = require('../lib/index')
const parseURI = ursParser.uri
const parseURL = ursParser.url

describe('parseUniformResource', function() {
  it('should have tests written for it!')
})
